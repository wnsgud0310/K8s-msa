server:
  port: 8080

eureka:
  instance:
    prefer-ip-address: true
  client:
    register-with-eureka: true
    fetch-register: true
    service-url:
      defaultZone: http://eureka-service:8761/eureka
